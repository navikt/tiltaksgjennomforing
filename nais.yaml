apiVersion: "nais.io/v1alpha1"
kind: "Application"
metadata:
  name: tiltaksgjennomforing
  namespace: default
spec:
  image: navikt/tiltaksgjennomforing:latest
  team: teamtag
  port: 80
  healthcheck:
    liveness:
      path: /isAlive
      initialDelay: 20
    readiness:
      path: /isReady
      initialDelay: 20
